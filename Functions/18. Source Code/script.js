/* If you are calling the function without using round brakets,
then you'll see the source code instead of the result  */
function func() {
	return '!';
}

alert(func); // не пишем круглые скобки => function func() { return '!'; }